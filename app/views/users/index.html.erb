<h1>User Index</h1>

<% @users.each do |user| %>
    <%= user.username %>
    <%= user.email %>
    <%= link_to 'Edit', edit_user_path(user) %>
    <%= link_to 'Destroy', user_path(user),
    method: :delete,
    data: { confirm: 'Are you sure?' } %>
    <br/>
<% end %>